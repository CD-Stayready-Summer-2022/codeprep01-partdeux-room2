package com.codedifferently.problems;

public class Problem01 {
    /**
     *
     * @param input the string to be searched
     * @param letter the letter that you will search for
     * @return
     */
    public Integer countDuplicateLetters(String input, String letter){
        //First step is change uppercase to lowercase
        // declare char turning letter into lowercase
        // Then convert to a char n assign to declare char on line 12
        // Declare a (char int) HashMap
        // add char letter to HashMap n assign value 0
        // for loop that goes through each char in the string input
        // if current char is in map then increment the count
        // return the value associated with the one entry
        return null;
    }
}
