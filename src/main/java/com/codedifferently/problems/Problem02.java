package com.codedifferently.problems;

import java.util.ArrayList;

public class Problem02 {
    public static Integer cousinTag(ArrayList<String> cousins, Integer startIndex, String targetName){
        // Create a variable for target index
        // create a variable from distance of left n right of the index
        // check to see if targetIndex is greater than startIndex
        //          Distance of the left = cousins.length - (targetIndex, startIndex)
        //          Distance from right = targetIndex - startIndex
        // Else
        //  distance from left = startIndex - targetIndex
        //  distance from right = cousin.size - (targetIndex, startIndex)
        // return the min distance from left and right

        return null;
    }
}
